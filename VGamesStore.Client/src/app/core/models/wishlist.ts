import { WishlistItem } from "./wishlist-item";

export interface Wishlist{
  id: string;
  items: WishlistItem[];
  // totalPrice: number;
  // totalItems: number;
  // createdAt: Date;
  // updatedAt: Date;
}
